# Copy to .env and fill in real values. Never commit .env.
PORT=8080
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/queens_touch

# Generate with: openssl rand -hex 32  (REQUIRED in production)
JWT_SECRET=
# How long issued sessions stay valid (https://www.npmjs.com/package/ms)
JWT_EXPIRES_IN=7d